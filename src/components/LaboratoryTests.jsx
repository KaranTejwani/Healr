const LaboratoryTests = () => {
    // First of all run the Server
    // then use the 'http://localhost:5000/api/laboratories' api to fetch the laboratories
    // Each laboratory contains key value pairs and there is a property named tests which contains an array of tests offered by that laboratories
    // For further information open the mongodb compass and look there in laboratories collection to understand the schema
    return(
        <>
        </>
    )
}

export default LaboratoryTests